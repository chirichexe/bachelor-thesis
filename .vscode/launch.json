{
    "configurations": [
        {
            "name": "Docker Node.js Launch",
            "type": "docker",
            "request": "launch",
            "preLaunchTask": "docker-run: debug",
            "platform": "node",
            "node": {
                "package": "${workspaceFolder}/docker/node_container/package.json",
                "localRoot": "${workspaceFolder}/docker/node_container"
            }
        }
    ]
}